# ============================================
# Toolbox 部署环境变量模板
# 复制为 deploy/.env 并填入真实值（deploy/.env 已被 gitignore）
#
# 本文件只服务本地备用部署脚本 `bash deploy/deploy.sh`；
# GitHub Actions 生产部署不读取本文件，其目标与凭据全部来自
# 仓库 Settings → Environments → production 的 Secrets：
#   TAILSCALE_OAUTH_CLIENT_ID / TAILSCALE_OAUTH_CLIENT_SECRET
#   VPS_SSH_KEY / VPS_HOST / VPS_USER / VPS_PORT / VPS_WWW
#   CLOUDFLARE_ACCOUNT_ID / CLOUDFLARE_API_TOKEN / CLOUDFLARE_PAGES_PROJECT
# 真实主机、端口、路径与密钥不得写入仓库。
# ============================================

# VPS 目标（仅 deploy/deploy.sh 读取）
VPS_HOST=your-vps-tailscale-hostname
VPS_PORT=22
VPS_WWW=/path/to/www/on/vps

# VPS 上的 Caddy 反代配置见 deploy/Caddyfile.example（占位符模板），
# 真实站点域名与容器 web 根目录只保留在私有部署配置中。
